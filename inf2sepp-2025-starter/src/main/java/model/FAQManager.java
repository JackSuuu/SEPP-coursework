package model;

import java.util.LinkedList;
import java.util.List;

// TODO: finish this class implementation
public class FAQManager {

    // private final FAQ faq = new FAQ();

    // // Get the FAQ object
    // public FAQ getFAQ() {
    //     return faq;
    // }

    // // Add a new FAQ section
    // public void addSection(String sectionTitle, String sectionContent) {
    //     FAQSection section = new FAQSection(sectionTitle, sectionContent);
    //     faq.addSection(section);
    // }

    // // Remove an FAQ section by title. Returns true if removal is successful.
    // public boolean removeSection(String sectionTitle) {
    //     List<FAQSection> sections = faq.getSections();
    //     FAQSection toRemove = null;
    //     for (FAQSection section : sections) {
    //         if (section.getSectionTitle().equals(sectionTitle)) {
    //             toRemove = section;
    //             break;
    //         }
    //     }
    //     if (toRemove == null) {
    //         System.out.println("Section " + sectionTitle + " not found");
    //         return false;
    //     } else {
    //         sections.remove(toRemove);
    //         return true;
    //     }
    // }

    // // View all FAQ sections as a string
    // public String viewFAQ() {
    //     List<String> result = new LinkedList<>();
    //     for (FAQSection section : faq.getSections()) {
    //         result.add(section.toString());
    //     }
    //     if (result.isEmpty()) {
    //         return "\nNo FAQ sections available in the system now\n";
    //     } else {
    //         return result.toString();
    //     }
    // }

}